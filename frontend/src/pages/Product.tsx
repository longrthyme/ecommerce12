


const Product: React.FC<any> = ({ id, name, price, image, quantity }) => {
    return (
        <div className="flex items-center justify-between p-4 border-t border-b border-gray-200">
            product
        </div>
    );
};

export default Product;
